server:
  port: 8080

spring:
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: "replace-with-a-strong-256-bit-secret-value-very-long-random"
  # if you also issue tokens here; otherwise keep only for parsing/validation
  expiration-ms: 3600000
